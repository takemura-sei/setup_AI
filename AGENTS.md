# AGENTS.md

このファイルは、このリポジトリで作業するAIエージェント向けの行動指針です。
人間向けの紹介は [README.md](README.md) を参照してください。

## このリポジトリについて

Claude Code をはじめとした AI エージェント向けの拡張機能（Skills / サブエージェント / MCP設定 など）を作成・蓄積していくリポジトリです。

## ディレクトリ構成

現時点ではまだ雛形は存在しないが、今後は以下の構成で育てていく想定。

```
skills/<skill-name>/SKILL.md   … Claude Code Skills（1スキル1ディレクトリ、assets/ 等を同梱可）
agents/<agent-name>.md         … サブエージェント定義
mcp/                           … MCPサーバー設定・メモ
```

- 命名は kebab-case
- ディレクトリ構成が変わったら、この節を更新する

## 拡張機能のライフサイクル（最重要）

- このリポジトリで作った拡張機能は、**まずこのリポジトリ内で管理する**（`~/.claude/skills/` などのグローバル設定に直接は作らない）
- **全体（複数プロジェクト）で使えると判断した場合のみ**、ユーザーが**手動で**グローバル設定へ移動する
- エージェントは、明示的な指示がない限り、拡張機能を勝手にグローバル設定へコピー・移動してはならない（「これはグローバルに向いていそう」という提案はしてよいが、実行するかどうかはユーザーが判断する）

## 新しい拡張機能を追加する手順

1. issueを立てる（本リポジトリもissue駆動の運用を踏襲。#2〜#11が実例）
2. `skills/<name>/` または `agents/<name>.md` を作成
3. 対象プロジェクトで動作確認する
4. README.mdの一覧（あれば）を更新する
5. PRを作成する

## 執筆・設計規約

- 各拡張機能には「何をするものか」「呼び出しタイミング」「ユーザー確認が必要な操作」を明記する
- 破壊的・外向きの操作（リポジトリ作成、push、既存ファイルの上書き、グローバル設定への配置など）は必ずユーザー確認を挟む設計にする
- 会話やリポジトリ内に既出の情報は聞き直さない
- 完了時は「次のアクション」を明示し、拡張機能単体で完結させない
